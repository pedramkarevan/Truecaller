package truecaller.confige;

import java.util.Properties;

/**
 * Every functionality related to configuration could be defined
 */
public interface IConfiguration {
    public Properties propertyConfig(Properties properties);
}
